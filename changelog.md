## 1.0.0
- Fixed Create not being listed as a dependency.
- Fixed missing homepage and issue URLs in `mods.toml`.
- Created new icon and added it to `mods.toml`.

<br>

## 0.1.0
- All Create Redstone Link frequencies will now also check for data components (NBT).
- Toggleable via server config.
